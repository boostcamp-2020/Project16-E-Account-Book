import React from 'react';
import styled from 'styled-components';

const checkBoxWithNumber: React.FC = () => {
  return <CheckBoxWithNumber />;
};

// CheckBoxWithNumber.defaultProps = defaultProps;

export default checkBoxWithNumber;
