import React from 'react';
import styled from 'styled-components';

const UserInviteCard = styled.div`
  background-color: green;
`;

const userInviteCard: React.FC = () => {
  return <UserInviteCard />;
};

export default userInviteCard;
