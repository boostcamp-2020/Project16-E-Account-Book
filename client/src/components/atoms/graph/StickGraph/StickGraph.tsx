import React from 'react';
import styled from 'styled-components';

interface Props {
  percent: number;
  money: number;
  name: string;
}

const StickGraph: React.FC<Props> = ({ percent, money, name }: Props) => {
  return <StickGraph />;
};

export default StickGraph;
